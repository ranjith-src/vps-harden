# vps-harden configuration file
# Usage: sudo vps-harden --config config.env

# Required
username=deploy
ssh_key=ssh-ed25519 AAAA...

# Optional
ssh_safety_ip=203.0.113.10
# netbird_key=nbs-XXXX
timezone=UTC
# hostname=my-vps
auto_reboot=true
# openclaw_skill=true
# skip=sops,netbird
# only=prereqs,user,ssh,firewall

# Agent workspace hardening (enables agent_secrets, agent_webhook_auth, agent_logging, agent_data modules)
# agent_dir=/home/deploy/.openclaw
# webhook_port=5000
# agent_data_dir=/home/deploy/.openclaw/workspace/health
